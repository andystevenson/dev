{
  "scripts": {
    "start": "npx slinkity --incremental --serve",
    "build": "npx slinkity",
    "clean": "rm -rf _site .11ty*",
    "proxy": "npx netlify-cms-proxy-server"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.1",
    "slinkity": "^0.8.3"
  },
  "dependencies": {
    "@andystevenson/11ty": "^0.1.3",
    "axios": "^0.27.2",
    "nodemailer": "^6.7.8"
  }
}
